inherits: "configs/base.yaml"

project:
  group: bootstrap
  tags:
    - smoke
    - bicubic

dataset:
  family: kaggle
  name: kaggle_4x
  pairing_mode: paired
  scale_factor: 4

model:
  kind: bicubic

evaluation:
  split: "val"
  sample_limit: 8
  save_limit: 8
