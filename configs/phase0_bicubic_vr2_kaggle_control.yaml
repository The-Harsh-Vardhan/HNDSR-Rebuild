inherits: "configs/base.yaml"

project:
  group: "vR.2-control"
  tags:
    - vR.2
    - scratch
    - kaggle
    - control
    - bicubic

tracking:
  mode: "offline"
  notes: "vR.2 Kaggle control-lane readiness baseline"

dataset:
  family: kaggle
  name: kaggle_4x
  pairing_mode: paired
  scale_factor: 4

data:
  patch_size: 64
  batch_size: 2
  num_workers: 0
  val_limit: 8

model:
  kind: bicubic

evaluation:
  sample_limit: 8
  save_limit: 8
  grid_name: "vR.2_kaggle_control_grid.png"
